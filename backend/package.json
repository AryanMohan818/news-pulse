{
  "name": "backend",
  "version": "1.0.0",
  "description": "News Pulse API",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "postinstall": "pip install --no-cache-dir -r ../scraper/requirements.txt || pip3 install --no-cache-dir -r ../scraper/requirements.txt || true"
  },
  "dependencies": {
    "better-sqlite3": "^11.1.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}